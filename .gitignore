node_modules
dist
dist-ssr
*.local
.env
.env.*
*.key
*.pem
*.p12
*.pfx
.DS_Store
src-tauri/target
src-tauri/gen
.vscode
.idea
*.log
dist-before/

# Visual-audit renders. The harness regenerates these on demand;
# committing ~20MB of PNGs per phase would dwarf the source tree.
# The measurements next to them (metrics.json, probe.json) are tracked.
design-audit/**/*.png
