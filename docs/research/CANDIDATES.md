# Candidate rulings

| Verdict | Name | Kind | Why |
|---|---|---|---|
| STEAL-THE-IDEA | CSS Paint API (Houdini paint() worklet) | platform-feature | The effect is worth building; the mechanism is not.

I verified the good parts empirically. paintWorklet runs in the harness Chromium, the worklet loads under this app's exact CSP (script-src 'self') with zero violations |
| REJECT | background-clip: border-area | platform-feature | The feature is real (WebKit shipped it; csswg-drafts #9456), costs zero bytes, and violates none of constraints 1, 2, 3, 4, 6 or 7. It is rejected because its central argument is factually false, it is architecturally bl |
| REJECT | oklab / oklch gradient interpolation (`linear-gradient(in oklab, …)`) | css-technique | Verified against the real stylesheet, not the claim. It passes every hard constraint — no package, no WebGL, no WASM, no CSP change (6), no network (7), no imagery (1), no Qur'anic text touched (2), no runtime cost (5),  |
| ADOPT-SCOPED | Build-generated tiled grain / blue-noise texture | css-technique | VERDICT RATIONALE

There is no package to vet — this is a technique, not a dependency. Nothing on npm, no WebGL context, no WASM, no network fetch, no runtime JS. Constraints 3, 5, 6 (script-src) and 7 are passed trivial |
| REJECT | OffscreenCanvas + Web Worker for the tier-3 field | platform-feature | The mechanical claims check out and are irrelevant; the two claims that would justify the change do not survive measurement.

CSP and cost claims verified. `tauri.conf.json` sets `script-src 'self'`, and worker-src falls |
| REJECT | CSS scroll-driven animations (animation-timeline: scroll() / view()) | platform-feature | Rejected not on a hard constraint but because the headline claim is false as applied, and the effect contradicts the app's own documented ambient rule. I tested it rather than reasoning about it, in Chromium 141 at /opt/ |
| REJECT | CSS 3D layered parallax (perspective + translateZ) | css-technique | Not a package — zero bytes, no npm entry, no WebGL, no WASM, no fetch. So constraints 4, 6 and 7 are all non-issues, and constraints 1/2/3 are untouched (gradients and geometry depict nothing). The proposal fails on mech |
| STEAL-THE-IDEA | View Transitions API (document.startViewTransition) | platform-feature | VERIFIED CLAIMS. Zero bundle bytes: true. No WASM, no eval, no data: URI, no network — constraints 6 and 7 pass cleanly against the CSP in src-tauri/tauri.conf.json (`script-src 'self'`). No imagery, no audio, no colour  |
| ADOPT-SCOPED | prefers-reduced-transparency | platform-feature | VERIFIED, NOT TRUSTED. This is a platform media query, not a package — so the usual grounds for rejection do not exist. There is nothing on npm to size, no colour object, no WebGL context, no WASM, no fetch. Constraint 5 |
| REJECT | Progressive blur (stacked backdrop-filter with mask ramps) | css-technique | VERIFICATION FIRST — the headline claim is factually wrong about this repo.

"For the poster rails, where content currently just clips at the container edge, this is a real improvement." It does not clip. `src/index.css: |
| ADOPT-SCOPED | simplex-noise (npm) | npm-package | VERIFIED. Exists at 4.0.3, MIT (LICENSE in tarball), unpacked 108,997 B (~109KB, claim accurate), `dependencies` and `peerDependencies` both empty, last publish 2024-07-26T17:06:40Z. Pure math: returns a scalar in [-1,1] |
| REJECT | @paper-design/shaders-react | npm-package | Verified against the real tarballs (npm pack, 0.0.78) and this repo's actual config, not the submission's summary. The submission is honest and reaches the right answer; verification confirms it and adds a disqualifier i |
| REJECT | css-doodle | tool | The package is real and the size/licence numbers check out: css-doodle@0.51.0, published 2026-04-19, MIT, zero runtime deps, dist.unpackedSize 327,521 bytes / 53 files (css-doodle.min.js = 101,864 bytes). It also contain |
| REJECT | WebGPU | platform-feature | Verified, and the submission's own conclusion holds — but two of its supporting claims needed correcting, and one new disqualifier turned up.

WHAT I VERIFIED
1. Support claim is accurate. Downloaded `web-features@3.34.2 |
| — | Motion 12 (in-place upgrade of framer-motion 11 → 12.42.2) | npm-package | Four things in v12 are directly better for this app's lane, not merely different. (1) 12.34.0 + 12.35.0: `useScroll`/`scroll` are now hardware-accelerated and can be driven by native ScrollTimeline **and ViewTimeline** — |
| ADOPT-SCOPED | Element-scoped View Transitions (Element.startViewTransition, Chrome 147) | platform-feature | VERIFIED CLAIMS. The feature is real: Element.startViewTransition() shipped Chrome/Edge 147 (April 2026), supports concurrent and nested transitions, scopes pseudo-elements to the subtree so they respect ancestor clips/t |
| ADOPT-SCOPED | CSS scroll-driven animations (animation-timeline: view() / scroll()) | platform-feature | VERIFIED, NOT TRUSTED. This is a platform feature, so the usual npm checks are moot: 0 bytes, no WebGL, no WASM, no data: URI, no network — constraints 5, 6 and 7 are untouched by the mechanism itself. Constraint 4 passe |
| REJECT | CSS sibling-index() / sibling-count() | platform-feature | No constraint kills this — it is 0 bytes, pure CSS, no WebGL (5), no WASM or data: URI (6), no network (7), and it touches nothing under 1, 2 or 3. Support is real: Chrome/Edge 138 June 2025, Safari 26.2, so WebView2 eve |
| STEAL-THE-IDEA | offset-path: rect(… round …) + offset-distance travelling beam | css-technique | VERIFIED, AND THE CENTRAL PERFORMANCE CLAIM IS FALSE.

The claim "pure CSS keyframes, composited" does not survive measurement. I ran the technique in the project's own Chromium (/opt/pw-browsers/chromium, 141 — the ever |
| ADOPT-SCOPED | CSS anchor positioning + popover + interestfor | platform-feature | Split the candidate: adopt the positioning half (anchor/anchor-size/@position-try/position-area/popover), reject `interestfor`, and forbid both near the mushaf.

WHY THE POSITIONING HALF PASSES — verified, not trusted. I |
| ADOPT-SCOPED | corner-shape: superellipse() / squircle | platform-feature | VERIFIED, NOT TRUSTED. I tested this in Chromium 141 (/opt/pw-browsers/chromium-1194) with a pixel-decoding harness against a reproduction of the app's real `.glass` primitive. Scripts and screenshots in /tmp/claude-0/-h |
| ADOPT-SCOPED | @property-registered angle for the existing conic rim | css-technique | MECHANISM VERIFIED CLEAN. Measured in Chromium 141 (WebView2-equivalent): `CSS.registerProperty` present, `--glow-angle` resolves as typed `140deg`, so the "unregistered properties snap instead of interpolate" diagnosis  |
| STEAL-THE-IDEA | motion-primitives | copy-paste-gallery | VERIFIED FACTS (not taken on trust).

npm: the package DOES exist — `motion-primitives@0.1.0`, MIT, but `bin: motion-primitives`, deps commander/node-fetch/ora, 20.0 kB unpacked, ONE version, last published 2025-03-04 an |
| STEAL-THE-IDEA | Magic UI | copy-paste-gallery | VERIFICATION (claims checked, not trusted)

Packaging. `magicui` does not exist on npm (E404). `@magicuidesign/cli@1.0.3` is real but 9,593 bytes unpacked with deps ora/zod/chalk/inquirer/commander — an installer, not a  |
| STEAL-THE-IDEA | Aceternity UI | copy-paste-gallery | Verified, not trusted. npm `aceternity-ui@0.2.2` is real but is a 2024-era scaffolding CLI (last publish 2024-07-22, 26 transitive deps including `posthog-node` telemetry and `node-fetch`), not a runtime library — nothin |
| REJECT | @dnd-kit/core + @dnd-kit/sortable | npm-package | This is the cleanest candidate reviewed so far and it violates no hard constraint. I am rejecting it on necessity and sequencing, not quality.

VERIFIED CLAIMS. Both packages are real and MIT: @dnd-kit/core 6.3.1 (publis |
| STEAL-THE-IDEA | @atlaskit/pragmatic-drag-and-drop | npm-package | The package is real (2.0.1, Apache-2.0, 462 KB unpacked / 333 files, deps: @babel/runtime, bind-event-listener, raf-schd). No WebGL, no WASM, no data: URI, no network — constraints 5, 6, 7 all pass, and 1/2/3 are not eng |
| STEAL-THE-IDEA | react-parallax-tilt | npm-package | Claims verified and honest: unpacked the tarball, 10,050 B raw / 2,926 B gzip exactly as stated, MIT, zero runtime deps (peer deps react/react-dom only), v1.7.335 published 2026-07-26. No WebGL, no WASM, no network fetch |
| ADOPT-SCOPED | Element-scoped View Transitions (Element.startViewTransition) + view-transition-name: match-element | platform-feature | VERIFIED TRUE. Element.startViewTransition() shipped Chrome 147 stable; view-transition-name: match-element shipped Chrome 137 (also Firefox 144, Safari 18.4). It is a platform API, not a package — there is nothing to np |
| — | Container scroll-state queries — @container scroll-state(scrollable | stuck | snapped) | platform-feature | Concretely deletes code you have. src/components/ui/Rail.tsx carries useState + useRef + useEffect + a scroll listener whose whole job is to compute atStart/atEnd and stamp data-hidden on .rail-fade — and its own comment |
| REJECT | CSS carousel pseudo-elements — ::scroll-button() and ::scroll-marker() / scroll-marker-group | platform-feature | Verified against /home/user/salafi-video-hub/src/components/ui/Rail.tsx, index.css lines 2374-2495, and the CSP in src-tauri/tauri.conf.json. The candidate is honest about what the feature is — it is a real platform feat |
| REJECT | background-clip: border-area | platform-feature | The feature is real (WebKit shipped it; csswg-drafts #9456), costs zero bytes, and violates none of constraints 1, 2, 3, 4, 6 or 7. It is rejected because its central argument is factually false, it is architecturally bl |
| ADOPT-SCOPED | @property — registered typed custom properties | platform-feature | Nothing to check on npm — this is a CSS at-rule, not a package. 0 bytes, no WebGL, no WASM, no data: URI, no network, Chrome 85+ so WebView2 is fine. It passes constraints 6 and 7 cleanly and there is no dependency to st |
| STEAL-THE-IDEA | CSS spring easing via linear() (build-time, postcss-spring-easing) | tool | VERIFIED CLAIMS. Package is real: postcss-spring-easing@1.0.2, MIT, 38.5KB unpacked, 12 files — but last published 2023-06-09, three years unmaintained. Deps postcss, postcss-value-parser, spring-easing@2.3.3 (221KB, las |
| ADOPT-SCOPED | popover attribute + CSS anchor positioning (anchor-name, position-area, position-try-fallbacks) | platform-feature | VERIFIED AGAINST THE PLATFORM, NOT THE PITCH. Not an npm package, so there is nothing to size or audit for hardcoded colour. I probed the exact binary the visual harness drives (`/opt/pw-browsers/chromium`, Chromium 141. |
| REJECT | interpolate-size: allow-keywords / calc-size() | platform-feature | Nothing here violates a constraint — it fails because there is no call site and the premise is false.

VERIFIED CLEAN. Real platform feature, no npm package, 0 bytes, no supply chain. No WebGL context and no GPU allocati |
| ADOPT-SCOPED | CSS gap decorations — column-rule / row-rule on grid and flex (+ *-rule-inset) | platform-feature | VERIFIED CLAIMS. Chrome 149 stable landed 2 June 2026; WebView2 Runtime 149.0.4022.62 followed 10 June 2026 — so the feature is ~7 weeks old in the runtime this app actually ships on. `src-tauri/tauri.conf.json` sets `we |
| ADOPT-SCOPED | corner-shape: superellipse() / squircle / bevel / notch / scoop | platform-feature | Verified as a platform property, not a package — no npm entry, 0 bytes, no WebGL context, no WASM, no data: URI, no network. Constraints 5, 6 and 7 pass trivially, which is the only reason this escapes the default REJECT |
| STEAL-THE-IDEA | CSS @function + if() with style() queries + sibling-index(), and color-mix(in oklab) | platform-feature | Take the audit finding; discard all four proposed syntaxes. The diagnosis is right and nothing else is.

WHAT IS WORTH STEALING (constraint 4). The candidate is correct that index.css violates "ten themes recolour with Z |
| REJECT | Scroll-driven (animation-timeline) and scroll-triggered (animation-trigger) animations | platform-feature | This is two features with different problems, and neither survives.

VERIFICATION (ran the real check, did not trust the claims). I probed `/opt/pw-browsers/chromium` — the exact binary CLAUDE.md mandates for visual veri |
| REJECT | backdrop-filter: url(#svg-filter) — correction to the KokonutUI rejection | platform-feature | The correction is factually right and worth recording; the technique still does not ship.

VERIFIED — the capability claim holds. WebKit bug 245510 is open ("backdrop-filter: url(#some-svg-filter) doesn't work with SVG f |
| — | Hand-rolled SVG + a 25-line monotone-cubic path generator | css-technique | MEASURED, not estimated. I built the identical chart six ways against one React baseline (esbuild-minified, gzip -9). Hand-rolled: +2,575 B raw / +1,201 B gzip. recharts: +354,039 B raw / +100,881 B gzip. That is 84x the |
| — | recharts 3 (the incumbent) — verdict | npm-package | It is NOT better, and the adoption was the wrong call for THIS chart. Two concrete reasons beyond size. (1) recharts 3 rewrote its internals onto a Redux stack: its hard dependencies are @reduxjs/toolkit, react-redux, im |
| — | d3-shape + d3-scale (no wrapper) | npm-package | Measured +25,632 B raw / +9,564 B gzip — 10.5x cheaper in gzip than recharts for identical output, and it is the exact code recharts calls underneath (via victory-vendor). Tree-shakes cleanly; area(), line(), curveMonoto |
| — | visx (@visx/shape + @visx/scale + @visx/curve) | npm-package | Measured +38,604 B raw / +13,885 B gzip — 7x cheaper in gzip than recharts. Renders plain SVG you fully control, so colours are whatever you pass, meaning `rgb(var(--token))` works and ten-theme recolour is free. No JS t |
| — | uPlot | npm-package | Not better here, and the popular "12 KB gzip" figure did not survive measurement in a React app: +52,328 B raw / +22,031 B gzip plus 708 B of CSS — larger than visx and 2.3x the d3-shape route. Its real win is redrawing  |
| — | Observable Plot | npm-package | Not better. Measured +272,525 B raw / +89,470 B gzip — within 11% of recharts. It bundles most of d3 because the grammar can express any mark. |
| — | shadcn/ui Charts and Tremor | copy-paste-gallery | Actively worse — they ADD to recharts rather than replace it (Tremor is cited around 200 KB vs recharts' ~150 KB). Their headline feature is CSS-variable chart theming, which this app ALREADY does by hand, correctly, via |
| — | chart.ts | reference | It does not exist. I checked the registry: `chart.ts` is a 433-byte EMPTY STUB published in 2019 by an unrelated maintainer (qkveri), last touched 2022, no description. The chartts.com blog promoting it is SEO spam. Flag |
| — | feDropShadow (single-primitive glow) | platform-feature | VERIFIED in Chromium: flood-color="rgb(var(--a-rgb))" resolves to the live theme colour (computed rgb(212,175,55)) and renders a real coloured glow. One primitive instead of two, and — the actual win — it DECOUPLES the g |
| — | CSS relative color syntax + color-mix() | platform-feature | This is the strongest library-free win available, and it is aimed squarely at a footgun CLAUDE.md already documents. The `-rgb` triplet convention exists only so alpha can be applied, and it carries the documented `rgba( |
| — | The existing CSS-grid streak heatmap — verdict: keep | css-technique | Already correct — this is the finding, not a recommendation to change. The code comment ('recharts has no calendar and faking one from a scatter costs more than 20 lines of grid') is right, and every calendar-heatmap npm |
| — | Nivo | npm-package | Not better — fails constraint 4 structurally. Nivo is themed through a JS theme OBJECT passed as a prop (axis, grid, tooltip, labels as nested JS), not through CSS. Ten themes would mean either ten JS theme objects or a  |
| — | TradingView Lightweight Charts | npm-package | Not applicable. It is purpose-built for financial OHLC/candlestick time series; an area chart is possible but the whole API (crosshairs, price scales, time scales, panes) is built around trading semantics that would need |
| — | Tufte sparkline principles (design reference, not a dependency) | reference | Confirms the current design is already close to state of the art and identifies the one gap. Both axes hidden, no legend, a large direct-labelled figure (totalDays / streak) beside the curve, and endpoint date labels bel |
| — | native-feel-skill — 75-item ship-readiness audit | reference | This is the single highest-value item in my lane and it is exactly the 'tells of an amateur desktop UI' list you asked for — it exists, it is concrete, and it is auditable. I pulled the full checklist (checklists/ship-re |
| — | Fluent material hierarchy — Mica for windows, Acrylic ONLY for transient surfaces | reference | This is a taste correction, not an addition, and it is the sharpest critique I can offer of what already ships. The app's `.glass` primitive (backdrop-filter: blur(18px) saturate(132%)) is applied to *content* surfaces — |
| — | tauri-apps/window-vibrancy — Mica / Acrylic backdrop | tool | It is the only way to satisfy audit item 31 (platform material) truthfully — CSS backdrop-filter cannot see the desktop behind the window, only the app's own content, so the app's current 'glass' is glass over itself. Mi |
| — | Windows 11 Snap Layouts on a custom titlebar (tauri-plugin-decoration / -frame / -snap-layout) | tool | This is a confirmed, concrete red. src-tauri/tauri.conf.json sets `decorations: false`, so the app draws its own titlebar — which means hovering the maximize button shows nothing, where every native Windows 11 app shows  |
| — | Restoring Win 11 rounded corners + drop shadow on an undecorated window | platform-feature | Pairs with the Snap Layouts gap and comes from the same root cause. Audit items 35 and 36 say 'no CSS box-shadow for window shadow' and 'no CSS border-radius for window corners' — the OS should draw both, at the exact pl |
| — | CSS relative colour syntax — oklch(from var(--accent) l c h / 0.16) | css-technique | It directly retires the load-bearing invariant documented in CLAUDE.md. The app currently maintains a parallel universe of `--x-rgb` channel triplets purely so `rgb(var(--x-rgb) / 0.16)` can produce alpha variants — and  |
| — | CSS Overflow L5 — ::scroll-button() and ::scroll-marker() on the poster rails | css-technique | This is a genuine upgrade to something the app already does well rather than a replacement for it. The rails are already native overflow + scroll-snap, which was the right call — this adds the two things that approach is |
| — | cmdk — command palette | npm-package | Confirmed absent: no cmdk, no command-palette component anywhere in src/, though src/hooks/useKeyboardShortcuts.ts exists. A command palette is the most load-bearing shared trait of every app on your reference list — Ray |
| — | backdrop-filter contending with video decode | reference | Not better than anything — it is a risk audit of what already ships, and it is the finding most likely to already be costing you something you have not attributed. The brief says video plays simultaneously and treats Web |
| — | content-visibility: auto + contain-intrinsic-size | css-technique | The zero-dependency answer to long lists — video grids and mushaf pages — that avoids pulling in a virtualiser and avoids managing scroll position or item heights by hand. It also does not fight scroll-snap, which a wind |
| — | Standard scrollbar properties — scrollbar-width / scrollbar-color | css-technique | Marginal, and I want to be straight about that. src/index.css styles ::-webkit-scrollbar at lines 735–749. On macOS that would be a real defect — giving ::-webkit-scrollbar an explicit width forces Chromium out of overla |
| — | Emil Kowalski's design-engineering skill (motion rules) | reference | Honestly: mostly it confirms this app is already there, which is worth knowing before spending effort. Its recommended curves are cubic-bezier(0.23,1,0.32,1) and the iOS drawer curve cubic-bezier(0.32,0.72,0,1) — the app |
| — | Scroll-driven animations (animation-timeline: scroll() / view()) | css-technique | Weaker for this app than its reputation suggests, and I would rank it last of the techniques here. The usual pitch is 'replace your scroll listeners', but this app does not have scroll-listener-driven animation — framer- |
| — | View Transitions API — recommending AGAINST | platform-feature | It is not better, and I am listing it so it gets ruled out on purpose rather than adopted later because it is fashionable. This is the obvious next reach for an app with react-router-dom and a motion budget, and it is th |
